import 'package:flutter/material.dart';

import 'custom_info_bar.dart';

class DummyInfoBar extends StatelessWidget implements CustomInfoBar {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

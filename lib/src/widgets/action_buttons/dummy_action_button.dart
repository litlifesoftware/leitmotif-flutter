import 'package:flutter/material.dart';
import 'custom_action_button.dart';

class DummyActionButton extends StatelessWidget implements CustomActionButton {
  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}

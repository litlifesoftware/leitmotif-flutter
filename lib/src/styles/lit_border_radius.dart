import 'package:flutter/painting.dart';

/// A Leitmotif `styles` class containing border radius members for various
/// use cases.
class LitBorderRadius {
  /// The default border radius applied on button.
  static const BorderRadius button = const BorderRadius.all(
    Radius.circular(16.0),
  );
}
